---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: registry
  namespace: registry
spec:
  hosts:
    - "registry.xplaceinmyheart.ru"
  gateways:
    - registry
  http:
    - match:
        - port: 80
      route:
        - destination:
            host: harbor
            port:
              number: 80
    - match:
        - port: 443
      redirect:
        port: 80
